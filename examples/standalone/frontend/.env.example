# =============================================================================
# Chat UI - Standalone Mode (No Authentication)
# =============================================================================

# LangGraph Server URL
NEXT_PUBLIC_API_URL=http://localhost:2024

# Auth Mode
AUTH_MODE=standalone
NEXT_PUBLIC_AUTH_MODE=standalone
